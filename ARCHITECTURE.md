# Smart Canteen - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        PRESENTATION LAYER                        │
│                         (Frontend - HTML/CSS/JS)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  index.html  │  │  login.html  │  │register.html │          │
│  │  Landing Page│  │  User Login  │  │ Registration │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  admin.html  │  │user_dash.html│  │user_order.html│         │
│  │Admin Dashboard│ │ Menu/Cart    │  │Order History │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐                             │
│  │ payment.html │  │order_conf.html│                            │
│  │   Payment    │  │ Confirmation │                             │
│  └──────────────┘  └──────────────┘                             │
│                                                                   │
│                    ┌──────────────┐                              │
│                    │   app.js     │                              │
│                    │  (Frontend   │                              │
│                    │   Logic)     │                              │
│                    └──────┬───────┘                              │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            │ HTTP/JSON (Fetch API)
                            │
┌───────────────────────────▼──────────────────────────────────────┐
│                      APPLICATION LAYER                            │
│                    (Backend - Flask/Python)                       │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│                        app.py (Flask App)                         │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    API ENDPOINTS                          │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                           │   │
│  │  User Management:                                        │   │
│  │  • POST /api/users/register                              │   │
│  │  • POST /api/users/login                                 │   │
│  │                                                           │   │
│  │  Menu Management:                                        │   │
│  │  • GET    /api/menu                                      │   │
│  │  • GET    /api/menu/<id>                                 │   │
│  │  • POST   /api/menu          (Admin)                     │   │
│  │  • PUT    /api/menu/<id>     (Admin)                     │   │
│  │  • DELETE /api/menu/<id>     (Admin)                     │   │
│  │                                                           │   │
│  │  Order Management:                                       │   │
│  │  • POST  /api/checkout                                   │   │
│  │  • GET   /api/orders?user_id=X                          │   │
│  │  • GET   /api/orders?admin=true                         │   │
│  │  • PATCH /api/orders/<order_id>/status  (Admin)        │   │
│  │                                                           │   │
│  │  Statistics:                                             │   │
│  │  • GET   /api/stats          (Admin)                    │   │
│  │                                                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              DATABASE CONNECTION                          │   │
│  │              (psycopg2)                                   │   │
│  └──────────────────┬───────────────────────────────────────┘   │
└────────────────────┼──────────────────────────────────────────────┘
                     │
                     │ SQL Queries
                     │
┌────────────────────▼──────────────────────────────────────────────┐
│                       DATA LAYER                                   │
│                  (PostgreSQL Database)                             │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐│
│  │  users           │  │  menu_items      │  │  orders         ││
│  ├──────────────────┤  ├──────────────────┤  ├─────────────────┤│
│  │• id (PK)         │  │• id (PK)         │  │• id (PK)        ││
│  │• username (UQ)   │  │• item_name       │  │• order_id (UQ)  ││
│  │• email (UQ)      │  │• price           │  │• user_id (FK)   ││
│  │• password        │  │• category        │  │• items (JSONB)  ││
│  │• role            │  │• description     │  │• total_amount   ││
│  │• created_at      │  │• availability    │  │• status         ││
│  │                  │  │• image_url       │  │• payment_method ││
│  │                  │  │• created_at      │  │• payment_status ││
│  │                  │  │• updated_at      │  │• transaction_id ││
│  │                  │  │                  │  │• created_at     ││
│  │                  │  │                  │  │• updated_at     ││
│  └──────────────────┘  └──────────────────┘  └─────────────────┘│
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

### User Registration Flow
```
User Form (register.html)
    ↓ (async POST request)
app.js: registerUser()
    ↓ (fetch /api/users/register)
Flask: api_register()
    ↓ (INSERT INTO users)
PostgreSQL: users table
    ↓ (return user data)
Flask: JSON response
    ↓ (success callback)
Frontend: Redirect to login
```

### Order Placement Flow
```
User adds items to cart (localStorage)
    ↓
User proceeds to payment
    ↓ (async POST request)
app.js: placeOrder()
    ↓ (fetch /api/checkout)
Flask: checkout()
    ↓ (INSERT INTO orders)
PostgreSQL: orders table
    ↓ (generate order_id)
Flask: JSON response with order details
    ↓ (success)
Frontend: Show confirmation, clear cart
```

### Menu Management Flow (Admin)
```
Admin views menu (admin.html)
    ↓ (async GET request)
app.js: getMenu()
    ↓ (fetch /api/menu)
Flask: api_get_menu()
    ↓ (SELECT * FROM menu_items)
PostgreSQL: menu_items table
    ↓ (return menu data)
Flask: JSON response
    ↓ (render in UI)
Frontend: Display menu items

Admin adds/edits item
    ↓ (async POST/PUT request)
app.js: addMenuItem() / updateMenuItem()
    ↓ (fetch /api/menu)
Flask: api_add_menu_item() / api_update_menu_item()
    ↓ (INSERT/UPDATE menu_items)
PostgreSQL: menu_items table
    ↓ (return updated data)
Flask: JSON response
    ↓ (refresh UI)
Frontend: Update menu display
```

## 🔐 Authentication Flow

```
User enters credentials
    ↓
Frontend validates input
    ↓
POST /api/users/login
    ↓
Backend validates credentials
    ↓
Query users table
    ↓
Match found?
    ├─ Yes → Return user data (id, username, email, role)
    │         ↓
    │         Store in localStorage
    │         ↓
    │         Redirect based on role
    │         ├─ admin → admin.html
    │         └─ user → index.html
    │
    └─ No → Return error message
              ↓
              Display error to user
```

## 🗄️ Database Relationships

```
users (1) ──────── (Many) orders
  ↑                           
  │ user_id (FK)              
  │                           
  └─────────────────────────┘

orders.items = JSONB [
  {
    id: (references menu_items.id conceptually),
    name: "Item Name",
    price: 100.00,
    quantity: 2
  },
  ...
]
```

## 🔄 State Management

### Frontend (LocalStorage)
```
smartCanteenCart        → Shopping cart items
smartCanteenCurrentUser → Logged-in user data
```

### Backend (PostgreSQL)
```
users       → User accounts
menu_items  → Menu catalog
orders      → Order records
```

## 🌐 API Communication Pattern

```
Frontend (JavaScript)
    ↓
    ↓ HTTP Request (JSON)
    ↓ Headers: Content-Type: application/json
    ↓
Backend (Flask)
    ↓
    ↓ SQL Query (Parameterized)
    ↓
Database (PostgreSQL)
    ↓
    ↓ Result Set
    ↓
Backend (Flask)
    ↓
    ↓ HTTP Response (JSON)
    ↓ Status Code (200, 201, 400, 404, 500)
    ↓
Frontend (JavaScript)
    ↓
    ↓ Update UI / Show Message
    ↓
User Interface
```

## 🚀 Technology Stack Layers

```
┌─────────────────────────────────────────┐
│           PRESENTATION                   │
│  HTML5 • CSS3 • JavaScript (ES6+)       │
└─────────────────────────────────────────┘
                   ↕
┌─────────────────────────────────────────┐
│          APPLICATION                     │
│  Flask • Python 3.8+ • CORS             │
└─────────────────────────────────────────┘
                   ↕
┌─────────────────────────────────────────┐
│         DATA ACCESS                      │
│  psycopg2 • SQL                         │
└─────────────────────────────────────────┘
                   ↕
┌─────────────────────────────────────────┐
│           DATABASE                       │
│  PostgreSQL 12+                         │
└─────────────────────────────────────────┘
```

## 📦 Deployment Architecture (Production)

```
┌──────────────────────────────────────────────────────────┐
│                    LOAD BALANCER                          │
│                    (Nginx/Apache)                         │
└────────────────────┬─────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        ↓                         ↓
┌───────────────┐         ┌───────────────┐
│  App Server 1 │         │  App Server 2 │
│  (Gunicorn)   │         │  (Gunicorn)   │
└───────┬───────┘         └───────┬───────┘
        │                         │
        └────────────┬────────────┘
                     ↓
        ┌────────────────────────┐
        │  PostgreSQL Database   │
        │  (with replicas)       │
        └────────────────────────┘
```

---

**This architecture provides:**
- ✅ Separation of concerns
- ✅ RESTful API design
- ✅ Scalable structure
- ✅ Clear data flow
- ✅ Easy maintenance
- ✅ Production-ready foundation
